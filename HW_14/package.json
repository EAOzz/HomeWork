{
  "name": "hw_14",
  "version": "1.0.0",
  "description": "1st cypress test",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cy:run:electron": "npx cypress run --browser electron",
    "cy:run:chrome:electron": "npx cypress run --browser chrome && npx cypress run --browser electron",
    "cy:run:dashboard": "npx cypress run --record --key 17b8b528-f9bb-458d-b865-0939f35c6809"
  },
  "keywords": [
    "cypress"
  ],
  "author": "EAOzz",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cypress": "^12.0.2"
  }
}
